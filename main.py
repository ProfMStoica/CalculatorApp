"""Represents the the starting point of the program that creates and runs the app"""

from mathapp import MathApp

#create the math app object
app = MathApp()

#call the run the method to run the application
app.run()